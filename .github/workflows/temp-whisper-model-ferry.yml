name: Temporary Whisper model ferry

on:
  push:
    paths:
      - '.github/workflows/temp-whisper-model-ferry.yml'
  pull_request:
    paths:
      - 'temp-trigger.txt'
  workflow_dispatch:

jobs:
  download-model:
    runs-on: ubuntu-latest
    steps:
      - name: Download public Whisper tiny.en q5_1 model
        run: |
          set -euo pipefail
          curl -L --fail --retry 5 --retry-delay 2 \
            'https://huggingface.co/ggerganov/whisper.cpp/resolve/main/ggml-tiny.en-q5_1.bin' \
            -o ggml-tiny.en-q5_1.bin
          test "$(stat -c%s ggml-tiny.en-q5_1.bin)" -eq 32166155
          sha256sum ggml-tiny.en-q5_1.bin
      - name: Upload model artifact
        uses: actions/upload-artifact@v4
        with:
          name: whisper-tiny-en-q5_1
          path: ggml-tiny.en-q5_1.bin
          compression-level: 0
          retention-days: 1
